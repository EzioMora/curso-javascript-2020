/* Conhecendo e entendo a tabela true e false dos operadores logicos

 Op logico '!' -> 'Negacao'

 !true -> false // Aqui estamos discendo que não verdadeiro é false
 !false -> true  // Aqui que não falso é verdadeiro

 Op logico '&&' -> 'e' // no op log "&&" ambos lados tem que ser verdadeiros para o resultado 
                         ser verdadeiro
 
 true && true -> true; 
 true && false -> false;
 false && ? -> false; // sim a primeira parte é false automaticamente o resultado é false

 Op logico '||' -> 'ou' // no op log '||' é preciso que um dos lado seja verdadeiro para
                           que o resultado final seja verdadeiro

 true || ? -> true
 false || true -> true
 false || false -> false


 Op logico 'xor -> 'ou exclusivo' no op log 'xor' ambos aldos tem quer diferentes para ser verdadeiro

 true xor true -> false
 true xor false -> true
 false xor true -> true
 false xor false -> false
*/