// conhecendo os operadores aritmeticos
// os operadores aritmeticos são operacoes binarias
const [a, b, c, d] = [3, 5, 1, 15]; // Atraves do operador destructuring criamos 4 constantes

const soma = a + b + c + d; 
const subtracao = d - b; 
const multiplicacao = a * b;  
const divisao = d / a;
const modulo = a % 2; // resto da divisao

console.log(soma, subtracao, multiplicacao, divisao, modulo);

// diferenca entre uma operacao binaria e unaria
// operador unario => -a  => -3  
// operador binario +> a + b => 8
